<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evast.evastcore" >

    <!-- 允许挂载和反挂载文件系统可移动存储-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity android:name=".SQLActivityDemo"/>
        <activity android:name=".PageActivityDemo"/>
        <activity android:name=".SlipActivityDemo"/>
        <service android:name=".service.ListenerNetService"/>
        <service android:name=".service.ServiceDemo"/>
    </application>

</manifest>
